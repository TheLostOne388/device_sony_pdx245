package vendor.semc.hardware.charger@1.2;

/**
 * Minimal stub interface for build purposes.
 * Method signatures might be needed later if hidl-gen fails
 * or other code needs to compile against this.
 */
interface ICharger {
    // Methods TBD
}; 